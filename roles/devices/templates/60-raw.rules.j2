{% for device in iscsi_devices %}
ENV{DM_UUID}=="mpath-{{ iscsi_device_ids[ansible_hostname][device.alias] }}", RUN+="/bin/raw {{ device.raw_character_device_path }} /dev/mapper/{{ device.alias }}"
KERNEL=="{{ device.raw_character_device_path | basename }}", ACTION=="add", OWNER="informix", GROUP="informix", MODE="0660", SYMLINK+="{{ device.alias }}"
{% endfor %}
